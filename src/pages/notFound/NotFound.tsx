import React from 'react'

const NotFound = () => {
  const style = {
    width: '100vw',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
  }
  return (
    <main style={style}>
      <h2>Страница не найдена</h2>
    </main>
  )
}

export default NotFound
